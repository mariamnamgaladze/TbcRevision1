 fun main() {
     val math = Math()
        math.lcm(n1 = readLine().toString().toInt(), n2 =readLine().toString().toInt() )
        math.gcd (n1 = readLine().toString().toInt(), n2 =readLine().toString().toInt() )
        math.containsDollarSignOrNot(string = readLine().toString())
        math.recursion(number = readLine().toString().toInt())
        math.reverse(Int = readLine().toString().toInt())
        math.polyndrom(num = readLine().toString() )
    }


